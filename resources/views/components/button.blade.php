<div class="{{ $parentClass ?? 'text-end' }}">
    <button type="{{ $type ?? 'button' }}" class="btn btn-sm rounded-0 shadow-none {{ $class ?? '' }}" id="{{ $id ?? '' }}"><span></span> {{ $label }}</button>
</div>
